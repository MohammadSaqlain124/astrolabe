# Runtime dependencies
skyfield>=1.54            # satellite propagation + sun/moon/eclipse astronomy
numpy>=1.26               # array math (also pulled in by skyfield)
SQLAlchemy>=2.0           # ORM / database access
httpx>=0.27               # HTTP client for fetching orbital data
APScheduler>=3.10         # scheduled refresh + alert jobs
python-dotenv>=1.0        # load secrets from a .env file
fastapi>=0.115            # web API layer
uvicorn[standard]>=0.30   # ASGI server that runs FastAPI
email-validator>=2.0      # EmailStr validation for /subscribe
